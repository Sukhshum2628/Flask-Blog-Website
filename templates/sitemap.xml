<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for url, date in pages %}
    <url>
        <loc>{{ url }}</loc>
        <lastmod>{{ date }}</lastmod>
        <priority>{% if "/post/" in url %}0.8{% else %}1.0{% endif %}</priority>
    </url>
    {% endfor %}
</urlset>
